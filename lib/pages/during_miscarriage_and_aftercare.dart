import 'package:flutter/material.dart';

class DuringMiscarriageAndAftercare extends StatelessWidget {
  const DuringMiscarriageAndAftercare({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("During Miscarriage And Aftercare"),
        backgroundColor: Colors.green,
      ),
    );
  }
}
