import 'package:flutter/material.dart';

class NaturalDietSuggestions extends StatelessWidget {
  const NaturalDietSuggestions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Natural Diet Suggestions"),
        backgroundColor: Colors.green,
      ),
    );
  }
}
